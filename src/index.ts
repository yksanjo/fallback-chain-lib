/**
 * Fallback Chain Library
 * 
 * Priority-based fallback execution
 * 
 * @module fallback-chain-lib
 */

export { FallbackChain, createFallbackChain } from './fallback-chain';
export type { Chainable, FallbackChainOptions } from './fallback-chain';
